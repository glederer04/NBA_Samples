# Dashboard Data

This folder will store the cleaned player-season dataset used by the NBA Player Comp Dashboard.

The app should load a local CSV instead of calling the NBA API every time it runs. This keeps the dashboard faster, more reliable, and easier to share.

## Main Dataset

Planned file:

```text
player_similarity_profiles.csv